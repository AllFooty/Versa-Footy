# SPA path proxies are handled by functions/_middleware.ts (Cloudflare
# Pages doesn't support external rewrites in _redirects).
#
# Root -> default locale. Accept-Language detection lived in proxy.ts;
# with static export we default to Arabic (primary GCC audience).
/                   /ar                                                            302
